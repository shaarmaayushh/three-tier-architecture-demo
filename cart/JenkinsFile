pipeline {
    agent any
    stages {
        stage('Debug') {
            steps {
                sh 'pwd' // Print current directory
                sh 'ls -la' // List files in current directory
            }
        }
        // Add other stages here
    }
}

