pipeline {
    agent any
    stages {
        stage('Debug') {
            steps {
                sh 'ls -la /var/lib/jenkins/workspace/SelfMade/'
            }
        }
        // Add other stages here
    }
}
