pipeline {
    agent any
    stages {
        stage('Debug') {
            steps {
                sh 'ls -la /var/lib/jenkins/workspace/SelfMade/cart'
            }
        }
        // Add other stages here
    }
}
